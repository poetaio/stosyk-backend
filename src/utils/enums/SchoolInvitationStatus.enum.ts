enum SchoolInvitationStatusEnum {
    PENDING = "PENDING",
    ACCEPTED = "ACCEPTED",
    WITHDRAWN = "WITHDRAWN",
    DECLINED = "DECLINED",
    EXPIRED = "EXPIRED",
}

export default SchoolInvitationStatusEnum;
